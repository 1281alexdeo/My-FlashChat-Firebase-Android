<?xml version="1.0" encoding="utf-8"?>
<manifest
    package="com.alexdeo.flashchatnewfirebase"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <!--Adding permission to access the internet-->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >

        <!--Activity 1-->
        <activity
            android:name="com.alexdeo.flashchatnewfirebase.LoginActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="stateHidden|adjustPan" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--Activity 2-->
        <activity
            android:name="com.alexdeo.flashchatnewfirebase.RegisterActivity"
            android:screenOrientation="portrait">
        </activity>

        <!--Activity 3-->
        <activity
            android:name="com.alexdeo.flashchatnewfirebase.MainChatActivity"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="stateHidden|adjustResize">
        </activity>

    </application>

</manifest>